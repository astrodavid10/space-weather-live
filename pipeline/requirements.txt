# The dome engine's requirements (pipeline/engine/requirements.txt is the record)
# plus test tooling. The storm/index/validate commands need only the stdlib.
numpy>=1.26,<3
scipy>=1.11
astropy>=5.3
requests>=2.31
geopack==1.0.13
hapiclient>=0.2.6,<0.4
pytest>=7
pyflakes>=3
